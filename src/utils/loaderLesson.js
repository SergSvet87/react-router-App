import { LESSONS_URL } from "./urls";

export const loaderLesson = (name) => {
  // const url = new URL(request.url);
  // const searchTerm = url.searchParams.get("lessonId");
  const lesson = () => lesson.name;
}
